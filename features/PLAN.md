Plan for repository feature specs

Goal
Create concise feature specifications that directly implement the capabilities in MISSION.md for the cron parsing and scheduling library.

Scope
Eight feature specs will be created under features/ to cover parsing, next-run computations, multi-run iteration, match checking, stringify, shortcuts, validation, and edge cases.

Tracking
Corresponding todos were inserted into the session database for tracking (parser, next-run, next-runs, match-check, stringify, shortcuts, validation, edge-cases).